function chemicalCompound(formula) {
    formula = formula(/[A-Z][a-z]*|\d+|[()]/g);
    let temp = [];

    if (formular.match){
        for (let i = 0; i < formula.length; i++) {
            
         }
    }
    
}